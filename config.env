# VM Blender Automation Configuration File
# Fill in your VM details below

# VM Connection Details (REQUIRED)
VM_HOST="194.26.196.132"  # Your VM's IP address or hostname
VM_USER="root"  # Your username on the VM
VM_PORT="21860"  # SSH port (default: 22)
VM_KEY=""   # Optional: path to SSH private key file

# Local Directories
LOCAL_INPUT_DIR="./input"
LOCAL_OUTPUT_DIR="./output"

# Remote Settings
REMOTE_WORK_DIR="/tmp/blender_work"

# Blender Installation
# Blender will be installed and executed via snap automatically.

# Blender Settings
# NOTE: BLENDER_FILE is now optional - script auto-detects all .blend files in input directory
# You can specify a single file with the -f flag if needed: -f filename.blend
OUTPUT_FORMAT="PNG"
FRAME_START=1
FRAME_END=200

# Cleanup Settings
CLEANUP_REMOTE=true